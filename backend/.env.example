# Copy this file to .env and fill in your values
# MongoDB: use a local URI or get a connection string from https://cloud.mongodb.com (MongoDB Atlas)

PORT=5000
MONGO_URI=mongodb://localhost:27017/automation-testing

# Example MongoDB Atlas URI (replace <user>, <password>, <cluster> with your values):
# MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/automation-testing?retryWrites=true&w=majority
